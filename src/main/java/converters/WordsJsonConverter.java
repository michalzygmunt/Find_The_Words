package converters;

import java.util.HashMap;
import java.util.Map;

public class WordsJsonConverter extends JsonConverter<Map> {
    public WordsJsonConverter(String jsonFilename) {
        super(jsonFilename);
    }
}
